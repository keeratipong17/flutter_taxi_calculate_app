name: flutter_taxi_calculate_app
description: "Taxi Calculate App"

publish_to: 'none' 


version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'


dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  font_awesome_flutter: ^10.8.0
  image_picker: ^1.1.2 # เปิดกล้อง และ เลือกจาก แกลเลอรี่
  intl: ^0.20.2 # จัดรูปแบบวันที่ เวลา ตัวเลข

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true
  assets:
    - assets/images/
  

